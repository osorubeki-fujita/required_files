require 'spec_helper'

describe RequiredFiles do
  it 'has a version number' do
    expect(RequiredFiles::VERSION).to eq( "0.2.0" )
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
